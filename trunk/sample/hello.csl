

$MY_NAME="Tyke Dog";

def check(opra, oprb)
{
   println("####INVOKED!");
   return $opra > $oprb;
}

def hello(opra, oprb)
{
   if check($opra, $oprb)
   {
      println("hello, opra > oprb");
   }
   else
   {
      println("hello, opra < oprb \na");
   }
   
    $x = 1+2+12;
    println($x);
    println($MY_NAME);
}