

$MY_NAME="Tyke Dog";

def check(opra, oprb)
{
   println("####INVOKED!");
   return $opra > $oprb;
}

def hello(opra, oprb)
{
   if check($opra, $oprb)
   {
      println("hello, opra > oprb");
   }
   else
   {
      println("hello, opra < oprb \na");
   }
   
   if $opra > 10
   {
       if $oprb >0 
       {
           println("eter here!");
       }
   }
    $x = 1+2+12;
    println($x);
    println($MY_NAME);
}